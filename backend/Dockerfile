CMD ["sh", "-lc", "echo STARTING; echo PORT=$PORT; env; ls -la /app; ls -la /usr/local/bin; which python || true; python --version || true; python -m pip show uvicorn || true; exec python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT}"]
